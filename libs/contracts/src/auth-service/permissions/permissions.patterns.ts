export const PERMISSIONS_PATTERNS = {
  CREATE: 'auth.createPermission',
  FIND_ALL: 'auth.permissions.findAll',
  BY_RESOURCE: 'auth.permissions.byResource',
  UPDATE: 'auth.permissions.update',
  DELETE: 'auth.permissions.delete',
} as const;
